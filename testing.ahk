testing(flan:=""){
	m(auto_update())
	/*
		list:=files.sn("//*[@encoding='CP0']")
		while,ll:=list.Item[A_Index-1]
			m(ll.xml)
		sc:=csc()
		;m(v.color.builtin)
		m(sc.2010(sc.2008))
		;Abs()
		sc.4004("fold",[1])
		m("here")
		return flan+1
	*/
}